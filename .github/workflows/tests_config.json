{
  "tests": [
    {
      "name": "Test directory structure",
      "setup": "",  # Optional setup script to run before the test
      "run": "pytest test_grade.py::test_dir_struc",  # Command to run the test
      "input": "",  # Optional input data for the test
      "output": "",  # Optional expected output to compare against
      "comparison": "included", # How to compare output (included, exact, etc.)
      "timeout": 10,  # Test execution timeout in seconds
      "points": 0.5  # Points awarded on successful test completion
    },
    {
      "name": "Test use of here or relative paths",
      "setup": "",
      "run": "pytest test_grade.py::uses_rel_path",
      "input": "",
      "output": "",
      "timeout": 10,
      "points": 0.5
    }
  ]
}